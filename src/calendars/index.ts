// Alternate calendar systems. Currently: Bikram Sambat (Nepal).
//
// Each calendar lives in its own subfolder and is opt-in: import directly
// from `@anilkumarthakur/d8/calendars/<name>` to avoid pulling its data
// table into bundles that don't need it.

export { BikramSambat } from './bs'
